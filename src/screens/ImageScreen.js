import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const ImageScreen = () => {
  return <Text style={styles.textSize}>Hello World From Image Screen</Text>
}

const styles = StyleSheet.create({
  textSize: 30
})

export default ImageScreen;
